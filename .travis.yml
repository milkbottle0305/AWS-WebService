language: java
jdk:
  - openjdk8

branches:
  only:
    - master
    -
before_install:
  - chmod +x gradlew

# Travis CI 서버의 Home
cache:
  directories:
    - '$home/.m2/repository'
    - '$home/.gradle'

script: "./gradlew clean build"

# CI 끝나면 메일로 알람
notifications:
  email:
    recipients:
      - err404@khu.ac.kr